# Example 11 - Bun.serve TLS Proxy

This demo issues an HTTPS request through the service worker proxy to a Bun.serve handler.

## Run

```bash
pnpm install
cd examples/11-bun-serve-tls
npx vite
```

Click "Run" to make the HTTPS request.
