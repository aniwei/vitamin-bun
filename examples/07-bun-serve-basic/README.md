# Example 07 - Bun.serve Basic

This demo starts a minimal Bun.serve handler and fetches from the browser via the service worker bridge.

## Run

```bash
pnpm install
cd examples/07-bun-serve-basic
npx vite
```

Click "Run" in the page to start the container and send a request.
