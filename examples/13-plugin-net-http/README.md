# Example 13 - Plugins for net/http/https

This demo registers a runtime plugin that overrides `http`/`https` and blocks `net`/`tls`.

## Run

```bash
pnpm install
cd examples/13-plugin-net-http
npx vite
```

Click "Run" to see the plugin output.
