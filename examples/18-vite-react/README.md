# Example 18 — Vite + React (BunContainer)

这个示例是一个 Vite + React 应用，界面通过 `@vitamin-ai/sdk` 创建 BunContainer，并在容器内执行脚本，展示 stdout/stderr。

## 运行

在仓库根目录：

- `pnpm install`
- `pnpm --filter @vitamin-ai/examples dev -- --open /18-vite-react/`

或在 examples 目录：

- `pnpm dev -- --open /18-vite-react/`
