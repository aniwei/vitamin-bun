# Example 09 - Bun.serve Streaming

This demo returns a streaming response and reads chunks in the browser.

## Run

```bash
pnpm install
cd examples/09-bun-serve-streaming
npx vite
```

Click "Run" to fetch the stream and print the chunks.
