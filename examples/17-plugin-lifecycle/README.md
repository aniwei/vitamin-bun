# Example 17 - Plugin Lifecycle

This demo shows runtime plugin lifecycle hooks and module interception.

## Run

```bash
pnpm install
cd examples/17-plugin-lifecycle
npx vite
```

Click "Run" to execute the plugin demo.
