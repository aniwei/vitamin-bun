# Example 08 - Bun.serve Routes

This demo shows basic routing, headers, and JSON responses using Bun.serve.

## Run

```bash
pnpm install
cd examples/08-bun-serve-routes
npx vite
```

Click "Run" in the page to issue two requests and print their results.
