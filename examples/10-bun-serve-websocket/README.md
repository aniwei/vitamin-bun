# Example 10 - Bun.serve WebSocket

This demo attempts a WebSocket connection through the service worker bridge. Behavior may vary by browser.

## Run

```bash
pnpm install
cd examples/10-bun-serve-websocket
npx vite
```

Click "Run" and check the output log.
