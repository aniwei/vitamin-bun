# Design Notes

## Hono Example
- Use Bun.serve as the HTTP entry point.
- Build a minimal Hono app with at least one JSON route and one text route.
- Keep dependencies lightweight; use existing Vite dev setup.

## Module Examples
- Prefer single-purpose examples with short READMEs.
- Ensure each example lists limitations when browser runtime differs from Node/Bun.
