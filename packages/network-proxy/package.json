{
  "name": "@aspect-build/network-proxy",
  "version": "0.0.1",
  "description": "Network proxy layer translating WASI sockets to browser fetch/WebSocket",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "typescript": "^5.7.0",
    "vitest": "^3.0.0"
  }
}
