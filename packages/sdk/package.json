{
  "name": "@aspect-build/sdk",
  "version": "0.0.1",
  "description": "Public API for embedding Bun-in-browser containers",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@aspect-build/browser-runtime": "0.0.1",
    "@aspect-build/virtual-fs": "0.0.1",
    "@aspect-build/network-proxy": "0.0.1"
  },
  "devDependencies": {
    "typescript": "^5.7.0",
    "vitest": "^3.0.0"
  }
}
