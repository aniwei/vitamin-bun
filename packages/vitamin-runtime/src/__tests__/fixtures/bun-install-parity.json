{
  "cases": [
    {
      "name": "core-single-dep",
      "dependencies": { "demo": "^1.0.0" },
      "expect": { "nodeModules": ["demo/index.js"], "lockfile": true }
    },
    {
      "name": "core-transitive",
      "dependencies": { "demo": "^1.0.0" },
      "expect": { "nodeModules": ["demo/index.js", "dep/index.js"], "lockfile": true }
    },
    {
      "name": "workspace-link",
      "dependencies": { "local-lib": "workspace:*" },
      "expect": { "nodeModules": ["local-lib/index.js"], "lockfile": true }
    }
  ],
  "notes": "Fixtures describe expected install outcomes for parity validation."
}
